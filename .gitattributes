* text=auto eol=lf

# Generated content: stored verbatim (CRLF from preprocess.mjs). Skip EOL
# renormalization so `git add content` doesn't re-check/convert 23k files.
content/** -text
data/*.jsonl -text
